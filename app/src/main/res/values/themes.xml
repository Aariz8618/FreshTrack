<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme (Material3) -->
    <style name="Base.Theme.FreshTrack" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Default/light customizations go here -->
        <!-- Example: <item name="colorPrimary">@color/action_green</item> -->
    </style>

    <!-- App theme -->
    <style name="Theme.FreshTrack" parent="Base.Theme.FreshTrack" />

    <!-- Fullscreen variant -->
    <style name="Theme.FreshTrack.FullScreen" parent="Theme.FreshTrack">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>